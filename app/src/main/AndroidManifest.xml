<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pehand.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".common.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.ServiceOrderActivity"
            android:label="@string/order_now"></activity>
        <activity android:name=".ui.SubServiceActivity" />
        <activity android:name=".ui.ServiceDetailsActivity" />
        <activity
            android:name=".ui.ContactUsActivity"
            android:label="@string/contact_us" />
        <activity android:name=".ui.OffersActivity" android:label="@string/offers" />
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>